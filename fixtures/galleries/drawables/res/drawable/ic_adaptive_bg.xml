<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive-icon background layer: a solid fill (opaque so masked corners are testable). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
  <solid android:color="@color/gallery_primary" />
</shape>
