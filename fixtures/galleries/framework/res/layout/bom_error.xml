<?xml version="1.0" encoding="utf-8"?>
<!-- DF-5 (HOST-05) AC2: a genuine mid-file syntax error (stray "a" after the attribute value,
     modeled on the reported file) that a leading BOM must not mask. The BOM'd file's real error
     must surface at its true line, never the "PI must not start with xml" @1:5 artifact. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"a
    android:layout_height="wrap_content">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="stray attribute character" />
</LinearLayout>
