<?xml version="1.0" encoding="utf-8"?>
<!-- LAY-08 AC7 shape (a): the first reported layout shape — a match_parent x wrap_content
     ConstraintLayout card with margins, background and padding, over the real bundled
     ConstraintLayout (2.2.1). -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginTop="16dp"
    android:background="#FF224488"
    android:padding="12dp">

    <View
        android:id="@+id/card_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/card_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Card title"
        app:layout_constraintTop_toBottomOf="@id/card_icon"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/card_body"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Card body text"
        app:layout_constraintTop_toBottomOf="@id/card_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>
